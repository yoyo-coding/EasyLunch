<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="StartPage.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StartPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="StartPage">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt" />
    </Window.SystemBackdrop>


    <Grid Padding="32" RowSpacing="24">
        <Grid.Resources>
            <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <local:StringNullOrWhiteSpaceToVisibilityConverter x:Key="IconVisibilityConverter" />
            <local:StringNullOrWhiteSpaceToVisibilityConverter x:Key="FallbackIconVisibilityConverter" Invert="True" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:ConnectionId='2' x:Name="AppTitleBar" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">
                <Button x:ConnectionId='8'
                    Width="40"
                    Height="32"
                    Padding="0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                                             
                    ToolTipService.ToolTip="刷新">
                    <SymbolIcon Symbol="Refresh" />
                </Button>
                <TextBlock Text="StartPage" FontSize="30" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>

            <AutoSuggestBox x:ConnectionId='7'
                x:Name="SearchBox"
                Grid.Column="1"
                Width="480"
                MaxWidth="560"
                Height="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                PlaceholderText="搜索应用..."
                QueryIcon="Find"
                                                    />
        </Grid>

        <Grid Grid.Row="1">
            <GridView x:ConnectionId='3'
                x:Name="AppsGrid"
                Padding="0,8,0,8"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                IsItemClickEnabled="True"
                                              
                ItemsSource="{Binding Apps}"
                SelectionMode="None">
                <GridView.ItemContainerTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition IsStaggeringEnabled="True" />
                        <RepositionThemeTransition />
                    </TransitionCollection>
                </GridView.ItemContainerTransitions>
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" ItemWidth="148" ItemHeight="152" />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="Margin" Value="4" />
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid x:ConnectionId='4'
                            Width="132"
                            Height="140"
                            Padding="8"
                                                                   
                                                                 
                                                                   
                                                                     
                            RenderTransformOrigin="0.5,0.5"
                            ToolTipService.ToolTip="{Binding Name}">
                            <Grid.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Grid.RenderTransform>
                            <Grid.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem x:ConnectionId='5' Text="打开文件位置" Tag="{Binding}"                                />
                                    <MenuFlyoutItem x:ConnectionId='6' Text="以管理员身份运行" Tag="{Binding}"                                  />
                                </MenuFlyout>
                            </Grid.ContextFlyout>

                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
                                <Border
                                    Width="78"
                                    Height="78"
                                    Padding="0"
                                    Background="Transparent"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="20">
                                    <Border.Shadow>
                                        <ThemeShadow />
                                    </Border.Shadow>
                                    <Grid>
                                        <FontIcon
                                            FontSize="34"
                                            Glyph="&#xECAA;"
                                            Opacity="0.68"
                                            Visibility="{Binding IconPath, Converter={StaticResource FallbackIconVisibilityConverter}}" />
                                        <Border
                                            CornerRadius="20"
                                            Visibility="{Binding IconPath, Converter={StaticResource IconVisibilityConverter}}">
                                            <Border.Background>
                                                <ImageBrush ImageSource="{Binding IconPath}" Stretch="Fill" />
                                            </Border.Background>
                                        </Border>
                                    </Grid>
                                </Border>
                                <TextBlock
                                    MaxWidth="116"
                                    HorizontalAlignment="Center"
                                    FontSize="13"
                                    Text="{Binding Name}"
                                    TextAlignment="Center"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="14"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <ProgressRing Width="48" Height="48" IsActive="{Binding IsLoading}" />
                <TextBlock Text="正在扫描应用..." Opacity="0.78" />
            </StackPanel>

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="10"
                Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                <FontIcon FontSize="52" Glyph="&#xE721;" Opacity="0.52" />
                <TextBlock Text="没有找到匹配的应用" FontSize="18" FontWeight="SemiBold" />
                <TextBlock Text="请换个关键词，或点击“刷新”重新扫描。" Opacity="0.7" />
            </StackPanel>
        </Grid>

        <TextBlock
            Grid.Row="2"
            HorizontalAlignment="Center"
            Opacity="0.68"
            Text="{Binding StatusMessage}" />
    </Grid>
</Window>




